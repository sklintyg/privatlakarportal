---
# file: group_vars/all

# General
inera_root: /opt/inera
java_home: "{{ inera_root }}/jre"
releases_folder: "{{ inera_root }}/releases"

# Spring profiles
profiles: test

# Configuration repository
environment_name: "test"
config_name: privatlakarportal-konfiguration
config_repository: https://github.com/sklintyg/privatlakarportal-konfiguration.git
config_folder: "{{ inera_root }}/privatlakarportal-konfiguration"
gitcrypt_key_name: "git-crypt4.key"
gitcrypt_key_file: "./{{ gitcrypt_key_name }}"
config_etc_folder: "{{ config_folder }}/{{ environment_name }}"

# Application resources
# Note: end with slash, in order to copy only files inside the source folder, not the folder itself
app_resources_src: "{{ playbook_dir }}/../src/main/resources/"
app_resources_folder: "{{ inera_root }}/privatlakarportal-resources"

# Tomcat installation
tomcat_root_folder: tomcat-priv
tomcat_service: privatlakarportal-tomcat

tomcat_conf: "{{ inera_root }}/{{ tomcat_root_folder }}/conf"
tomcat_bin: "{{ inera_root }}/{{ tomcat_root_folder }}/bin"
tomcat_lib: "{{ inera_root }}/{{ tomcat_root_folder }}/lib"
inera_tomcat_folder: "/var/lib/{{ tomcat_service }}"
webapps_folder: "{{ inera_tomcat_folder }}/webapps"

# Database configuration
database_server: localhost
database_port: 3306
database_name: privatlakarportal
database_username: inera
database_password: inera
database_driver: com.mysql.jdbc.Driver
database_url: "jdbc:mysql://{{ database_server }}:{{ database_port }}/{{ database_name }}?useCompression=true"
database_pool_maxActive: 20
database_pool_maxIdle: 20
database_pool_minIdle: 10
database_pool_initialSize: 10