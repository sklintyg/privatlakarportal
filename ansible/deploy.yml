---

- hosts: tomcat
  serial: 1
  become: yes
  environment:
    "{{ proxy_env }}"
  vars:
    # repo - values: 'snapshots or 'releases'
    repo: releases

  roles:
    - app-resources
    - privatlakarportal
