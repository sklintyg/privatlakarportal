CGI_ELEG_SAML_KEYSTORE_ALIAS=localhost
CGI_ELEG_SAML_KEYSTORE_FILE=file://${application.dir}/certifikat/localhost.p12
DB_NAME=${database.name}
DB_PORT=${mysql.service.port}
DB_SERVER=${mysql.service.host}
HSA_WS_CERTIFICATE_FILE=${application.dir}/certifikat/localhost.p12
HSA_WS_CERTIFICATE_TYPE=PKCS12
HSA_WS_SERVICES_URL=http://localhost:8080/services/hsa
HSA_WS_SERVICE_LOGICALADDRESS=
HSA_WS_TRUSTSTORE_FILE=${application.dir}/certifikat/truststore.jks
HSA_WS_TRUSTSTORE_TYPE=JKS
MAIL_ADMIN=admin@webcert.se
MAIL_FROM=no-reply@webcert.intygstjanster.se
MAIL_HOST=nmtmail.nordicmedtest.se
NTJP_BASE_URL=http://localhost:8080/services/stubs
NTJP_WS_CERTIFICATE_FILE=${application.dir}/certifikat/localhost.p12
NTJP_WS_CERTIFICATE_TYPE=PKCS12
NTJP_WS_TRUSTSTORE_FILE=${application.dir}/certifikat/truststore.jks
NTJP_WS_TRUSTSTORE_TYPE=JKS
PRIVATLAKARPORTAL_HOST_URL=<value>
PUTJANST_LOGICALADDRESS=PUTEST
REDIS_HOST=${redis.service.host}
REDIS_PORT=${redis.service.port}
REDIS_SENTINEL_MASTER_NAME=master
SPRING_PROFILES_ACTIVE=test,pp-security-test,hsa-stub,wc-stub,wc-hsa-stub,wc-pu-stub,mail-stub,testability-api,pp-init-data,caching-enabled
WEBCERT_HOST_URL=https://webcert.inera.nordicmedtest.se
WEBCERT_INTERNAL_HOST_URL=http://localhost:8080/services/stubs
WEBCERT_INTERNALAPI_URL=http://localhost:8120/internalapi
WEBCERT_WS_TRUSTSTORE_FILE=${application.dir}/certifikat/truststore.jks
WEBCERT_WS_TRUSTSTORE_TYPE=JKS