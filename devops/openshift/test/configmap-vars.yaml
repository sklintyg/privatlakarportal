apiVersion: v1
data:
  SPRING_PROFILES_ACTIVE: "test,pp-security-test,hsa-stub,wc-stub,wc-pu-stub,mail-stub,testability-api,pp-init-data,caching-enabled"
  HSA_WS_SERVICES_URL: "http://${app.name}:8080/services/hsa"
  PRIVATLAKARPORTAL_HOST_URL: "<value>"
  WEBCERT_HOST_URL: "https://webcert.inera.nordicmedtest.se"
  WEBCERT_INTERNAL_HOST_URL: "http://${app.name}:8080/services/stubs"
  LOGBACK_FILE: "classpath:logback-ocp.xml"
  REDIS_HOST: "${redis.service.host}"
  REDIS_PORT: "${redis.service.port}"
  REDIS_SENTINEL_MASTER_NAME: "master"
  MAIL_HOST: "nmtmail.nordicmedtest.se"
  DB_PORT: "${mysql.service.port}"
  DB_SERVER: "mysql"
  DB_NAME: "${database.name}"
  PUTJANST_LOGICALADDRESS: "PUTEST"
  HSA_WS_SERVICE_LOGICALADDRESS: ""
  NTJP_BASE_URL: "http://${app.name}:8080/services/stubs"
  NTJP_WS_CERTIFICATE_FILE: "${certificate.folder}/test.intygstjanster.sjunet.org.jks"
  NTJP_WS_TRUSTSTORE_FILE: "${certificate.folder}/truststore.jks"
  NTJP_WS_CERTIFICATE_TYPE: "JKS"
  NTJP_WS_TRUSTSTORE_TYPE: "JKS"
  WEBCERT_WS_TRUSTSTORE_FILE: "${certificate.folder}/webcert.jks"
  WEBCERT_WS_TRUSTSTORE_TYPE: "JKS"
  CGI_ELEG_SAML_KEYSTORE_FILE: "file://${certificate.folder}/test.intygstjanster.sjunet.org.jks"
  CGI_ELEG_SAML_KEYSTORE_ALIAS: "test.intygstjanster.sjunet.org"
  HSA_WS_CERTIFICATE_FILE: "${certificate.folder}/test.intygstjanster.sjunet.org.jks"
  HSA_WS_CERTIFICATE_TYPE: "JKS"
  HSA_WS_TRUSTSTORE_FILE: "${certificate.folder}/truststore.jks"
  HSA_WS_TRUSTSTORE_TYPE: "JKS"
kind: ConfigMap
metadata:
  name: privatlakarportal-test-configmap-envvar
type: Opaque
